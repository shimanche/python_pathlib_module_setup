# Byte-compiled / optimized files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
# 以下の行は、PyInstaller が生成するディレクトリとファイルを無視します
/dist/
/build/
/*.spec

# Jupyter Notebook
# 以下の行は、Jupyter Notebook が生成するディレクトリを無視します
.ipynb_checkpoints

# IPython
# 以下の行は、IPython のログを無視します
profile_default/
ipython_config.py

# pipenv
# 以下の行は、pipenv が生成するファイルを無視します
Pipfile.lock

# virtualenv
# 以下の行は、virtualenv が生成するディレクトリを無視します
venv/
ENV/

# IDEs
# 以下の行は、一般的な IDE の設定ファイルを無視します
.idea/
.vscode/
